
(ns io.valid
    (:require [iso.io.valid :as valid]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (iso.io.valid/*)
(def valid-relative-path valid/valid-relative-path)
(def valid-absolute-path valid/valid-absolute-path)
