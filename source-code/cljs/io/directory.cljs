
(ns io.directory
    (:require [iso.io.directory :as directory]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (iso.io.directory/*)
(def directory-path->directory-name directory/directory-path->directory-name)
